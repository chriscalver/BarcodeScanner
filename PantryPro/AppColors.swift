import SwiftUI

extension Color {
    /// Shared app accent green (based on systemGreen). Use `.appGreen` everywhere to ensure consistent color across views.
    static let appGreen = Color(UIColor.systemGreen)
}
